services:
  backend:
    build: ./backend
    volumes:
      - db-data:/app/data
    environment:
      - DB_PATH=/app/data/jobs.db
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "3080:80"
    depends_on:
      - backend

volumes:
  db-data:
